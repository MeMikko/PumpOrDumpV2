export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-gradient-to-b from-gray-900 via-black to-gray-950">
      <section className="text-center space-y-4">
        <p className="text-sm uppercase tracking-[0.3em] text-indigo-400">Base x Farcaster</p>
        <h1 className="text-3xl font-semibold text-white">Mini App Shell Ready</h1>
        <p className="text-gray-400">Connect providers and build features with confidence.</p>
      </section>
    </main>
  );
}
